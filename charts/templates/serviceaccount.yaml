{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "utxoplugin.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
{{ include "utxoplugin.labels" . | nindent 4 }}
{{- end -}}
