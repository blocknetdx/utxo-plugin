apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "utxoplugin.fullname" . }}-test-connection"
  labels:
{{ include "utxoplugin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "utxoplugin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
